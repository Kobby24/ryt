DIRECTORY CLEANUP REPORT
========================

DIRECTORIES SCANNED:
3

USED DIRECTORIES:
3

UNUSED DIRECTORIES FOUND:
0

DIRECTORIES DELETED:
0

UNCERTAIN DIRECTORIES PRESERVED:
0

------------------------------------------------------------------------------
INVENTORY
------------------------------------------------------------------------------

After the HTML/CSS/JS/image migration, the repository root contains only:

  style/     CSS files (flat — no nested directories)
  script/    JavaScript files (flat — no nested directories)
  src/       image files (flat — no nested directories)

plus 16 HTML pages and .txt reports at the root.

A recursive directory listing found no child folders under style/, script/, or src/.
The old scrape dump folders (consarltd.com about-us/, Maps, gtag, fonts, CDN copies)
are already gone from this tree; they were emptied when assets were moved, then
removed as empty directories during migration.

------------------------------------------------------------------------------
DEPENDENCY GRAPH (from HTML/CSS/JS)
------------------------------------------------------------------------------

Root HTML (16 pages)
  -> style/normalize.css
  -> style/components.css
  -> style/consarltd.css
       -> ../src/*.jpeg|jpg|webp|svg  (background-image url())
  -> script/consarltd.js
  -> script/footer-year.js
  -> script/jquery-3.5.1.min.dc5e7f18c8.js   (index.html)
  -> script/west-africa-map.js               (index.html)
       -> fetch(data-svg-src) -> src/West-Africa.svg
  -> src/*  (img, srcset, favicon, data-svg-src)

External HTTPS (not local directories):
  fonts.googleapis.com
  googletagmanager.com
  d3e54v103j8qbb.cloudfront.net (jQuery on most pages)
  cdn.prod.website-files.com / uploads-ssl.webflow.com (service/project images)
  snap.licdn.com
  esm.sh (Three.js, imported by west-africa-map.js)

------------------------------------------------------------------------------
PRESERVED DIRECTORIES
------------------------------------------------------------------------------

Directory:
style/

Reason:
Referenced by HTML <link rel="stylesheet" href="style/...">.
CSS files inside also reference ../src/ images.

Verification:
Direct HTML references found on index, about-us, contact, and other local-styled pages.
Recursive CSS url() references point into src/, not into any other local folder.

---

Directory:
script/

Reason:
Referenced by HTML <script src="script/...">.
west-africa-map.js fetches src/West-Africa.svg (via HTML data-svg-src / fallback).

Verification:
consarltd.js, footer-year.js used by all 16 pages.
jquery and west-africa-map.js used by index.html.
Webpack chunk files live in the same folder; they are not a separate directory.
The script/ directory as a whole is required.

---

Directory:
src/

Reason:
Referenced by HTML <img>, srcset, favicon, data-svg-src, and by CSS url('../src/...').

Verification:
Direct HTML and CSS dependency paths into src/.
Unused files may exist inside src/; that does not make the directory unused.
Individual unused files were not deleted (directory-level cleanup only).

------------------------------------------------------------------------------
UNUSED DIRECTORIES
------------------------------------------------------------------------------

(none)

No directory exists in this repository that is unreferenced by the HTML/CSS/JS
dependency graph.

------------------------------------------------------------------------------
FINAL VALIDATION
------------------------------------------------------------------------------

Broken HTML references:
0  (local style/, script/, and primary src/ paths; missing srcset variants and
    never-dumped files such as webclip.jpg were already documented and are
    missing files, not missing directories)

Broken CSS references:
0  (paths point at src/; a few filenames were never dumped)

Broken JS references:
0

References to deleted directories:
0  (nothing was deleted in this pass)

No HTML, CSS, or JavaScript was modified.
No files were deleted.
No directories were deleted.
